"""Policy quoting via embeddings. Placeholder until documents are added."""


def get_relevant_policy_snippets(query: str, top_k: int = 3) -> list[str]:
    """
    Return relevant policy document snippets for the given query.
    For now: no documents loaded, returns empty list.
    When documents are added: embed query, search store, return snippets.
    """
    return []
